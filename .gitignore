*.iml
.gradle
/local.properties
# Every teammate's Android Studio writes different local IDE files here (run
# configs, gradle cache pointers, plugin migration flags) — ignore the whole
# folder instead of chasing each new file version by version.
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
.kotlin/
local.properties

# Firebase config — each teammate adds their own copy locally, never commit it
/app/google-services.json

# Submission export, never part of the source tree
*.zip
